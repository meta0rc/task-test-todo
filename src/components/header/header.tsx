import { Menu } from "../logo/logo"
import { HeaderStyles } from './style'
export const Header = () => {
    return(
        <header style={HeaderStyles}>
            <Menu />
        </header>

    )
}